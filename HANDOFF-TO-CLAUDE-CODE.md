# تسليم إلى Claude Code

هذا المشروع (Next.js على Cloudflare Workers + D1 عبر vinext) وصل إلى نقطة تم فيها بناء:

## ✅ تم إنجازه بالكامل (اختبرت صياغته syntactically، لكن لم أُثبّت الحزم ولم أُشغّله فعليًا لعدم توفر إنترنت في بيئتي)
- `db/schema.ts` — جداول users / sessions / projects
- `app/lib/auth.ts` — تشفير كلمات المرور (PBKDF2 عبر Web Crypto)، الجلسات، الكوكيز
- `app/lib/db-error.ts`, `app/lib/project-utils.ts`
- كل مسارات API: `app/api/auth/*`, `app/api/users/*`, `app/api/projects/*`
- `app/catalog-types.ts`, `app/catalog-view.tsx` — الأنواع ومكوّنات عرض الصفحات المشتركة (قابلة لوضع readOnly)
- `app/editor-client.tsx` — محرر المهندس الكامل (بديل app/page.tsx القديم)، متصل بالـ API بدل IndexedDB، ويحوي لوحة "الاعتماد وبيانات العميل" وشاشة "إدارة الحسابات"
- `app/page.tsx` — حارس صفحة يوجّه غير المسجّلين لـ /login والمصنع إلى /factory
- `app/setup/page.tsx` — إنشاء أول حساب مهندس (يعمل فقط إذا كان جدول users فارغًا)
- `app/login/page.tsx` — تسجيل الدخول بخيارين (مهندس / مصنع)
- `app/factory/page.tsx` + `app/factory/factory-client.tsx` — شاشة المصنع: قائمة المشاريع المعتمدة، تاريخ البداية/التسليم، المدة المتبقية، شريط تحديث نسبة الإنجاز، وعرض الكتالوج كاملاً (قراءة فقط قابل للطباعة)
- تنسيقات CSS جديدة أُضيفت في نهاية `app/globals.css` لكل الشاشات أعلاه
- `.openai/hosting.json` — عدّلت `"d1": null` إلى `"d1": "DB"` لتفعيل ربط قاعدة البيانات (تحقق من هذا يدويًا حسب لوحة الاستضافة الفعلية)

## ⚠️ لم يُختبر فعليًا — هذا أهم شيء تكمله أنت
لم يتوفر لي اتصال إنترنت لتشغيل `npm install` أو `npm run dev` أو `npm run build`، فكل الأكواد أعلاه اجتازت فحص **صياغة (syntax)** فقط عبر TypeScript compiler، وليس فحص أنواع (type-check) كامل ولا تشغيل فعلي.

### خطوات مطلوبة منك بالترتيب:
1. `npm run install:ci` (أو `npm install`)
2. `npm run db:generate` — لتوليد ملفات الهجرة (migration) في `drizzle/` من `db/schema.ts` الجديد
3. `npm run build` ثم أصلح أي خطأ TypeScript/ESLint يظهر (الأرجح: مشاكل بسيطة في الأنواع بين `editor-client.tsx` و `catalog-view.tsx`، أو في توقيع `context.params` في مسارات `[id]` — تأكد أنها متوافقة مع نسخة Next 16 المُستخدمة هنا بالضبط)
4. تأكد أن ربط D1 باسم `DB` مُفعّل فعليًا من لوحة الاستضافة (وليس فقط في hosting.json)
5. انشر (كما كنت تنشر سابقًا)، ثم افتح `/setup` لإنشاء أول حساب مهندس
6. من داخل المحرر، زر "الحسابات" لإنشاء حساب مصنع

### أشياء تحتاج مراجعة يدوية دقيقة (لم أستطع التحقق منها بدون تشغيل حقيقي):
- توقيع `context: { params: Promise<{ id: string }> }` في route handlers — هذا صحيح لنسخ Next الحديثة (15/16) لكن تأكد أنه متوافق مع `vinext` تحديدًا
- سلوك `Response.headers.append('Set-Cookie', ...)` عبر Cloudflare Workers runtime — يجب أن يعمل قياسيًا لكن اختبره فعليًا مع تسجيل الدخول والخروج
- لم أُنشئ زر حذف مشروع من واجهة "المشاريع الأخيرة" رغم وجود مسار DELETE جاهز في API — يمكن إضافته بسهولة إذا احتجته

## اقتراحات لتطوير لاحق (لم أنفذها، فقط اقتراحات)
- سجل نشاط (activity log) يوثّق من عدّل/اعتمد/حدّث الإنجاز ومتى
- حالات مشروع متعددة بدل ثنائية معتمد/غير معتمد (مسودة → مراجعة → معتمد → تحت التنفيذ → مكتمل → تم التسليم)
- رابط عرض للعميل نفسه (قراءة فقط) لمتابعة حالة مشروعه
- سجل تاريخي لنسبة الإنجاز (Timeline) بدل رقم واحد يُستبدل في كل مرة
- تنبيهات بريد إلكتروني عند الاعتماد أو اقتراب موعد التسليم
- إمكانية حذف مشروع من واجهة المهندس
